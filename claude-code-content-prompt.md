# פרומפט ל-Claude Code — הזרקת תוכן סופי לאתר אוצר היין

## הקשר
זהו פרויקט **Next.js (App Router) + TypeScript + Tailwind** קיים. אל תיצור פרויקט חדש ואל תשנה את הארכיטקטורה. עדכן **רק את התוכן והטוקנים** לפי הערכים הסופיים למטה. למידע עיצובי מלא קרא את `otzar-hayain-build-prompt.md` שבתיקיית השורש.

**אל תוסיף את אנימציית מזיגת היין (wine-pour) בשלב הזה.** שמור על המבנה הקיים; שנה רק מה שצריך כדי להכניס את התוכן.

---

## תוכן סופי (מאומת)

**1. מיקום/מיתוג — להבליט בכל מקום:**
זו **חנות אלכוהול ובר יין** (לא חנות יין בלבד). הצד הקמעונאי מוכר את כל הטווח — יין, וויסקי, ערק, ספיריטים — והבר הוא בר יין. אל תצמצם ל"חנות יין".

**2. צבע מותג:**
`--brand` = אדום־היין של הלוגו, `#7E2230` (לדגום את הגוון המדויק מקובץ הלוגו ב-`public/`). עדכן ב-`globals.css` / `tailwind.config.ts`. הצבע הזה משמש ל-CTA, ל-accent ולמוטיב החוזר.

**3. Hero eyebrow:**
`חנות בוטיק ליין ואלכוהול · בר יין` (הטאגליין הרשמי מהלוגו + הבר).

**4. כשרות:**
chip קטן `כשר` (pill, מסגרת ב-`--brand`, טקסט קטן) — אזכור בלבד, **לא section**. מקם בעדינות (hero meta או פוטר).

**5. FAQ:**
- `מה זה אוצר היין?` → `חנות אלכוהול ובר יין תחת קורת גג אחת, בלב אשדוד.`
- `איפה אתם?` → `שדרות תל חי 19, אשדוד.`
- `מה שעות הפתיחה?` → `ראשון–חמישי 11:00–23:00 · שישי 9:00–15:30 · שבת סגור`

**6. ביקור + פוטר:**
- כתובת: `שדרות תל חי 19, אשדוד, 7751013` (ספרות בתוך `<bdi>`)
- טלפון: `050-492-2311` → קישור `tel:+972504922311`
- וואטסאפ: `https://wa.me/972504922311`
- ניווט (כפתור): `https://www.google.com/maps/dir/?api=1&destination=31.7701526,34.6256714`
- שעות: כמו ב-FAQ
- אינסטגרם: `https://www.instagram.com/otzar_wine/`
- פייסבוק: `https://www.facebook.com/profile.php?id=61585669423608`
- `© 2026 אוצר היין`
- הערת גיל: `מיועד לבני 18 ומעלה. נא לשתות באחריות.`

**7. SEO + JSON-LD:**
- `metadata.title`: `אוצר היין | חנות אלכוהול ובר יין באשדוד`
- `JSON-LD LocalBusiness` (סוג `LiquorStore` + `BarOrPub`) ב-`layout.tsx`:
  name `אוצר היין` · address `שדרות תל חי 19, אשדוד, 7751013, IL` · geo `31.7701526, 34.6256714` · telephone `+972504922311` · openingHours `Su–Th 11:00–23:00 / Fr 09:00–15:30 / Sa closed` · `sameAs` = [אינסטגרם, פייסבוק] · `priceRange` → השאר `TODO` (טרם נקבע).

**8. RTL — לוודא:**
`lang="he" dir="rtl"`, Tailwind logical utilities בלבד (`ms/me`, `ps/pe`, `start/end`, `text-start`), `<bdi>` סביב טלפון/כתובת/מספרים, וכל קישור חיצוני עם `target="_blank" rel="noopener noreferrer"`.

---

## מה להשאיר כ-placeholder (אל תמציא)
- דומיין סופי (ל-canonical + OG)
- `priceRange` ל-JSON-LD
- הצהרת נגישות
- תמונות hero/DayTrio + גרסת לוגו שקופה (עד שיגיעו — `bg-surface` עם `// TODO`)

## מה לא לעשות
- לא להוסיף את אנימציית הכוס.
- לא להמציא תוכן חדש מעבר למה שכאן.
- לא לגעת ב-DB ולא לעשות deploy (אני עושה ידנית).

## פלט מבוקש
רשימת הקבצים ששונו + תקציר מה שונה בכל קובץ. אם נדרש שינוי מבני גדול — עצור ובקש אישור לפני.
